<div class="nested-fields statement-item-fields">
    <div class="item_from_list">
        <%= f.collection_select(:item_id, Item.order(:name), :id, :combined_value, { }, {:class=>'form-control', :id=>'item_desc'})  %>
    </div>
    <div class="margin-top margin-bottom">
        <%= link_to_add_association 'or create a new item', f, :item, class: 'btn btn-success' %>
        <%= link_to_remove_association 'Remove', f, class: 'btn btn-danger', :id=>'' %>
    </div>
</div>